"use strict";
/*
// trying Query database
app.use('/employees', (_req:Request , res: Response) => {
  console.log(_req.ip);
  pool.query('SELECT * FROM employee;', (err: Error, result: QueryResult) => {
    if (err) {
      console.log(err);
    } else if (result) {
      res.json(result.rows);
    }
  });
})


// Default response for any other request (Not Found)
app.use('/departments', (_req: Request , res: Response) => {
  console.log(_req.ip);
  pool.query('SELECT * FROM department;', (err: Error, result: QueryResult) => {
    if (err) {
      console.log(err);
    } else if (result) {
      res.json(result.rows);
    }
  });
});

app.use('/roles', (_req: Request , res: Response) => {
  console.log(_req.ip);
  pool.query('SELECT * FROM role;', (err: Error, result: QueryResult) => {
    if (err) {
      console.log(err);
    } else if (result) {
      res.json(result.rows);
    }
  });
});
*/ 
